(() => {
  // Compatibility layer kept intentionally passive.
  // All planner interactions and slide transitions are owned by script.js.
  // This prevents duplicate click handlers while preserving the existing UI.
})();
